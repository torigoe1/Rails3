<%= form_with url: room_search_path, local:true, method: :get  do |r| %>
    <%= r.label :area %><br/>
    <%= r.search_field :area %>
    <%= r.label :keyword %><br/>
    <%= r.search_field :keyword %>
    <%= r.submit "検索" %>
<% end %>

